{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the local Tomato Companion window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
